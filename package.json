{
  "name": "spotify-tracker",
  "version": "1.0.0",
  "description": "Spotify Music Recommendation Tracker",
  "main": "server/index.js",
  "scripts": {
    "start": "cd server && npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "build": "cd client && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../api && pip install -r requirements.txt",
    "test": "python test_simple.py"
  },
  "keywords": ["spotify", "music", "recommendations", "tracker"],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
