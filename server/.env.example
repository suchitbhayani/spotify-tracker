PORT=8080

# MongoDB Atlas URI (replace <username>, <password>, <cluster>)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>/spotify?retryWrites=true&w=majority

# Optional database name override (otherwise Atlas URI decides)
# MONGO_DB=spotify

# FastAPI ML service URL (leave blank until suchit’s API is live)
ML_SERVICE_URL=